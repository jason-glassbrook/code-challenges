#!python3


# Definition for doubly-linked list node.
class ListNode:

    def __init__(self, val=0, prev=None, next=None):

        self.val = val
        self.prev = prev
        self.next = next

        return
